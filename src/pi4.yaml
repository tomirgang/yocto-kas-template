header:
  version: 11
  includes:
    - scarthgap.yaml
    - default_config.yaml
    - dev_config.yaml

machine: raspberrypi4

local_conf_header:
  pi4-config: |
    # Allow wifi firmware license
    LICENSE_FLAGS_ACCEPTED = " synaptics-killswitch"
